Cloud readiness: The Supabase cards table and RLS were created. Signup via email and password, project URL configuration, confirmation links, email delivery, and cross-device tests must all be validated independently. Never treat a GitHub Pages green check as proof of database connectivity. Publishable key only in frontend, never service-role key. Local-only data must not be cleared on failed upload.
